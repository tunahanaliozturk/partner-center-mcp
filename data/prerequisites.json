{
  "version": "2026-08",
  "producers": [
    { "placeholder": "customer-id", "producedBy": "list-customers", "note": "Every customer-scoped route starts here. search-customers narrows by company name or domain." },
    { "placeholder": "subscription-id", "producedBy": "list-customer-subscriptions", "note": "Filter the result on status and offer to find the subscription you mean." },
    { "placeholder": "order-id", "producedBy": "list-customer-orders", "note": "checkout-cart also returns the order it created, which is usually the faster route." },
    { "placeholder": "user-id", "producedBy": "get-customer-users", "note": "The user list is the only place a user id comes from; there is no lookup by UPN." },
    { "placeholder": "cart-id", "producedBy": "create-cart", "note": "A cart id exists only after the cart is created, and carts expire." },
    { "placeholder": "product-id", "producedBy": "get-products", "note": "Pass the targetView and country you intend to sell in." },
    { "placeholder": "sku-id", "producedBy": "list-product-skus", "note": "SKUs are scoped to a product, a country and a target segment." },
    { "placeholder": "availability-id", "producedBy": "get-sku-availabilities", "note": "The availability is the third part of the catalogItemId a cart accepts." },
    { "placeholder": "offer-id", "producedBy": "list-offers", "note": "Legacy catalog. New Commerce purchases use a catalogItemId instead." },
    { "placeholder": "promotion-id", "producedBy": "get-promotions", "note": "Confirm the customer qualifies with verify-promotion-eligibility before relying on it." },
    { "placeholder": "invoice-id", "producedBy": "get-invoices", "note": "Filter by InvoiceDate to find the billing period you are reconciling." },
    { "placeholder": "transfer-id", "producedBy": "list-customer-transfers", "note": "create-transfer also returns the id of the transfer it created." },
    { "placeholder": "migration-id", "producedBy": "migrate-to-new-commerce", "note": "The id comes back from the migration POST; migration is asynchronous." },
    { "placeholder": "schedule-id", "producedBy": "create-migration-schedule", "note": "Returned when the schedule is created; get-migration-schedule reads it back." },
    { "placeholder": "entitlement-id", "producedBy": "get-azure-entitlements", "note": "An Azure plan subscription holds many entitlements, one per Azure subscription." },
    { "placeholder": "servicerequest-id", "producedBy": "list-service-requests", "note": "Service requests are listed per customer." },
    { "placeholder": "relationship-id", "producedBy": "get-gdap-relationships", "note": "Microsoft Graph, not Partner Center: the token audience differs." },
    { "placeholder": "access-assignment-id", "producedBy": "list-gdap-access-assignments", "note": "Access assignments hang off a GDAP relationship, so resolve the relationship first." },
    { "placeholder": "policy-id", "producedBy": "get-configuration-policies", "whenPathContains": "/policies", "note": "Autopilot configuration policies, listed per customer." },
    { "placeholder": "policy-id", "producedBy": "list-selfserve-policies", "whenPathContains": "/SelfServePolicy", "note": "Self-serve purchasing policies; entity_id is the customer tenant id." },
    { "placeholder": "devicebatch-id", "producedBy": "get-device-batches", "note": "Batches are listed per customer." },
    { "placeholder": "device-id", "producedBy": "get-batch-devices", "note": "Device ids come from the batch listing, not from the hardware hash you uploaded." },
    { "placeholder": "batchtracking-id", "producedBy": "create-device-batch", "note": "Returned in the Location header of the upload, not in its body." },
    { "placeholder": "line-item-number", "producedBy": "get-order-by-id", "note": "Line item numbers are positions within the order, starting at 0." },
    { "placeholder": "upgrade-id", "producedBy": "create-azure-plan-upgrade", "note": "The upgrade id is what get-product-upgrade-status polls." },
    { "placeholder": "growth-margin-id", "producedBy": "get-growth-margins", "note": "Growth margins are scoped to a country and segment." },
    { "placeholder": "agreement-template-id", "producedBy": "list-agreement-metadata", "note": "Quote the current templateId; a superseded template is rejected." },
    { "placeholder": "referral-id", "producedBy": "get-referrals", "note": "Referrals live on the pricing-and-referrals host with its own token audience." },
    { "placeholder": "role-id", "producedBy": "get-user-roles", "note": "Directory roles in the customer tenant. The same page documents the tenant-wide listing." },
    { "placeholder": "mpn-id", "producedBy": "get-partner-network-profile", "note": "Your own MPN id. For somebody else's, verify-mpn takes it as input rather than yielding it." },
    { "placeholder": "receipt-id", "producedBy": "get-invoice-by-id", "note": "Receipts hang off an invoice." },
    { "placeholder": "entity-id", "producedBy": "list-customers", "note": "The self-serve policy routes call the customer tenant id entity_id." }
  ]
}
